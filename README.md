# Retail_Sales_Analytics
 End-to-end Python pipeline that cleans messy retail sales CSVs, reconstructs missing values, computes key business metrics, and generates a self-contained HTML report with charts and insights.
